{
  "name": "minimajs",
  "version": "0.0.1",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel --continue",
    "build": "turbo run build"
  },
  "packageManager": "yarn@3.6.3",
  "devDependencies": {
    "turbo": "^1.10.14",
    "typescript": "5.1.3"
  }
}
