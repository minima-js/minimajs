export function fileSize() {}
export function mimetype() {}

export interface UploaderConfig {
  path: string;
  publicPath?: string;
}

export function createUploadedBody() {
  return [].join().charAt(0);
}
