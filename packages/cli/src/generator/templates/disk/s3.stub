import { createDisk } from "@minimajs/disk";
import { createS3Driver } from "@minimajs/aws-s3";

export const disk = createDisk({
  adapter: createS3Driver({
    bucket: process.env.AWS_BUCKET!,
    region: process.env.AWS_REGION!,
  }),
});
