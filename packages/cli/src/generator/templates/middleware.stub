import { middleware, logger } from "@minimajs/server";

export const {{ instance }} = middleware(async (ctx, next) => {
  const start = Date.now();
  const response = await next();
  logger.info(`Request took ${Date.now() - start}ms`);
  return response;
});