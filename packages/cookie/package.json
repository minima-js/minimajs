{
  "name": "@minimajs/cookie",
  "version": "1.1.0",
  "description": "Type-safe cookie management for MinimaJS",
  "type": "module",
  "exports": {
    ".": "./lib/index.js"
  },
  "scripts": {
    "dev": "tsc -w",
    "test": "bun test",
    "coverage": "bun test --coverage",
    "build": "yarn clean && tsc --build",
    "clean": "rimraf -g '*.tsbuildinfo' lib"
  },
  "devDependencies": {
    "@jest/globals": "^30.2.0",
    "@minimajs/server": "workspace:*",
    "@types/cookie": "^1.0.0",
    "@types/node": "^25.0.3"
  },
  "dependencies": {
    "cookie": "^1.0.2"
  },
  "peerDependencies": {
    "@minimajs/server": ">=0.3.0"
  },
  "files": [
    "lib"
  ]
}
